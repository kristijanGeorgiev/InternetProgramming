const weko = {
    name: "Wekoslav",
    lastName: "Stefanovski"
}


weko.name = "Weko";


const array = [1, 2, 3]
array.push('banana')


const add = (x, y) => x + y;
const mul = (x, y) => x * y;

function dosomething(first, second, operation) {
    return operation(first, second);
}