const weko = {
    name: "Wekoslav",
    lastname: 'Stefanovski'
}
weko.name = 'Wekoslav'
const array = [1,2,3]
array.push('Banana')
const add = (x, y) => x + y
const mul = (x, y) => x * y
function dosomething(first, second, operation) {
    return operation(first, second)
}