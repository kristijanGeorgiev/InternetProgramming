import { TooMuchDirective } from './too-much.directive';

describe('TooMuchDirective', () => {
  it('should create an instance', () => {
    const directive = new TooMuchDirective();
    expect(directive).toBeTruthy();
  });
});
